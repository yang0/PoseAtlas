Bundled contact sheet images for all 30 pages.

- Canonical page-to-image mapping is in `data/pages.json` and `data/page_images.json`.
- `assets/contact-sheets/*.webp` matches each page's `output_hint` / `image_path`.
- `assets/contact-sheets-png/*.webp` keeps the original delivered PNG pages with page-id and pose-range in the filename.
